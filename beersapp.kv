#:kivy 2.1.0

<RootLayout>:
    # Need to add links from object ids to properties (which can be access via self. in python code)
    text_in1: text_in1 # Assigns the call of display_label to an identifier within kv file
    # Can also directly access ids via <root element>.ids property
    BoxLayout:
        orientation: "vertical"
        BoxLayout:
            orientation: "horizontal"
            size_hint_y: 0.2 # Only take half of the allocated space in x-direction for inputs
            TextInput:
                id: text_in1 # ID assigns the identifier of this object in the root object in python (i.e., RootLayout)
                height: 10
            Button:
                text: "Check\ndrugs"
                height: 10
                size_hint_x: 0.2
                on_press: root.checkBeers()
            # Button:
            #     text: "Test1"
            #     height: 10
            #     size_hint_x: 0.1
            #     on_press: root.testFx1()
            # Button:
            #     text: "Test2"
            #     height: 10
            #     size_hint_x: 0.1
            #     on_press: root.testFx2()
            # Button:
            #     text: "Test3"
            #     height: 10
            #     size_hint_x: 0.1
            #     on_press: root.testFx3()
            # Button:
            #     text: "Test4"
            #     height: 10
            #     size_hint_x: 0.1
            #     on_press: root.testFx4()
        ScrollView:
            id: scroll_view
            do_scroll_x: False
            do_scroll_y: True
            TreeView:
                id: tree_view
                hide_root: True
                size_hint_y: None # Need to set this to none in order to use scrollview in the respective direction (x and y)
                height: self.minimum_height # Bind height (used for scroll) to minimum_height which changes when children are expanded
            # Accordion:
            #     id: accordion
            #     orientation: "vertical"
            #     size_hint_y: None # Need to set this to none in order to use scrollview in the respective direction (x and y)
            #     height: 50*8 # 50px for every element 